

@{
    ViewBag.Title = "Counseling";
}
<img class="fullscreen-video" src="@Url.Content("~/Content/Immagini/Slide/" + @ViewBag.Title + ".jpg")" />
@if (User.IsInRole("Admin"))
{
    <a href="@Url.Action("UploadSlide1", "Image", new{id = @ViewBag.Title})" title="Slide" class="btn btn-intro"><span class="fa fa-pencil fa-3x"></span></a>

}
<div class="div-counseling-top">
    <h2>
        In ogni organismo, uomo compreso, c’è un flusso costante teso alla realizzazione costruttiva delle sue possibilità intrinseche, una tendenza naturale alla crescita
    </h2>
    <h3 class="text-right">Carl Rogers</h3>



</div>
    <div class="div-counseling-bottom">
        <a href="@Url.Action("Counseling", "Home")">
            <h1 class="alert alert-fiordiloto">TeatroCounseling<span class="h5">®</span><span class="fa fa-angle-right"></span></h1>
        </a>
    </div>
